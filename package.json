{
  "name": "@parke.dev/pi-extensions",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo of pi coding agent extensions published as individual packages",
  "type": "module",
  "license": "MIT",
  "author": "Luke Parke",
  "homepage": "https://github.com/LukasParke/pi-extensions#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/LukasParke/pi-extensions.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write \"packages/**/*.{ts,md,json}\" \"*.{json,md}\" --ignore-path .prettierignore",
    "format:check": "prettier --check \"packages/**/*.{ts,md,json}\" \"*.{json,md}\" --ignore-path .prettierignore",
    "check": "npm run typecheck && npm run format:check && npm test",
    "pack:check": "npm pack --dry-run --workspaces --if-present && node scripts/check-integration-bundle.mjs",
    "release:check": "npm run check && npm run pack:check",
    "release:plan": "node scripts/release.mjs plan",
    "release:publish": "node scripts/release.mjs publish"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.84.1",
    "@earendil-works/pi-coding-agent": "^0.84.1",
    "@earendil-works/pi-tui": "^0.84.1",
    "@types/node": "^24.12.0",
    "prettier": "^3.4.2",
    "typebox": "^1.1.38",
    "typescript": "^5.9.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=22.19.0"
  },
  "overrides": {
    "adm-zip": "0.6.0"
  }
}
